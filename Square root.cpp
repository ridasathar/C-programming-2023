/* This program is used to find square root of a number */
# include <math.h>
# include <stdio.h>
int main()
{
	int a,b;
	scanf("%d",&a);
	b=sqrt(a);
	printf("The square root of a is %d",b);
	return 0;	
}
